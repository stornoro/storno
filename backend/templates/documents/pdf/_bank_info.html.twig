{% if (showBankInfo is not defined or showBankInfo) and (bankDisplaySection|default('both') == 'payment' or bankDisplaySection|default('both') == 'both') and pdf_visible('bank_account', labelOverrides|default(null)) %}
    {% if invoiceBankAccounts|default([])|length > 0 %}
        <div class="bank-info">
            <span class="label">{{ pdf_label('bank_account', labelOverrides|default(null), locale|default('ro'), 'bank_accounts') }}:</span>
            {% if bankDisplayMode|default('stacked') == 'inline' %}
                {% for account in invoiceBankAccounts %}
                    <span>{{ account.iban }}{% if account.bankName %} - {{ account.bankName }}{% endif %}</span>{% if not loop.last %} | {% endif %}
                {% endfor %}
            {% else %}
                {% for account in invoiceBankAccounts %}
                    <div>
                        {% if account.bankName %}<span>{{ account.bankName }}: </span>{% endif %}{{ account.iban }}
                    </div>
                {% endfor %}
            {% endif %}
        </div>
    {% elseif company.bankAccount %}
        <div class="bank-info">
            <span class="label">{{ pdf_label('bank_account', labelOverrides|default(null), locale|default('ro'), 'bank_account') }}:</span> {{ company.bankAccount }}
            {% if company.bankName %} - {{ company.bankName }}{% endif %}
            {% if company.bankBic %} (BIC: {{ company.bankBic }}){% endif %}
        </div>
    {% endif %}
{% endif %}
