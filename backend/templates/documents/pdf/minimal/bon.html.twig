{% extends 'documents/pdf/_base.html.twig' %}

{% block styles %}
body { font-size: 10px; color: #1a1a1a; }
.page { padding: 24px 32px; }

.header { overflow: hidden; margin-bottom: 20px; padding-bottom: 12px; border-bottom: 1px solid #e5e7eb; }
.header-left { float: left; }
.header-right { float: right; text-align: right; }
.doc-title { font-size: 20px; font-weight: bold; color: {{ primaryColor|default('#2563eb') }}; margin-bottom: 3px; letter-spacing: 0.5px; }
.doc-number { font-size: 11px; color: #555; margin-bottom: 2px; }
.doc-meta { font-size: 10px; color: #777; line-height: 1.6; }

.parties { overflow: hidden; margin-bottom: 18px; padding-bottom: 12px; border-bottom: 1px solid #e5e7eb; }
.party { width: 48%; }
.party-label { font-size: 9px; text-transform: uppercase; letter-spacing: 1px; color: #aaa; margin-bottom: 4px; font-weight: bold; }
.party-name { font-size: 12px; font-weight: bold; margin-bottom: 2px; color: #111; }
.party-detail { font-size: 9px; color: #666; line-height: 1.6; }

table.lines { width: 100%; border-collapse: collapse; margin-bottom: 16px; }
table.lines thead th { border-bottom: 1px solid #1a1a1a; padding: 5px 8px; text-align: left; font-size: 9px; text-transform: uppercase; color: #555; font-weight: 600; letter-spacing: 0.5px; background: none; }
table.lines tbody td { padding: 5px 8px; border-bottom: 1px solid #e5e7eb; font-size: 10px; color: #1a1a1a; }
table.lines tbody tr:last-child td { border-bottom: 1px solid #9ca3af; }

.payment-info { margin-top: 14px; padding: 8px 10px; border: 1px solid #e5e7eb; font-size: 9px; color: #555; }
.payment-info-title { font-weight: bold; margin-bottom: 3px; color: #333; }

.notes { margin-top: 14px; padding: 8px 10px; border: 1px solid #e5e7eb; font-size: 9px; color: #555; }
.notes-title { font-weight: bold; margin-bottom: 3px; color: #333; }

.footer { margin-top: 28px; padding-top: 10px; border-top: 1px solid #e5e7eb; font-size: 8px; color: #aaa; text-align: center; }
{% endblock %}

{% block content %}
    <div class="header">
        <div class="header-left">
            {% if (showLogo is not defined or showLogo) and logoDataUri %}
                <div class="header-logo"><img src="{{ logoDataUri }}" alt="Logo"></div>
            {% endif %}
            <div class="doc-title">{{ pdf_label('receipt_title', labelOverrides|default(null), locale|default('ro')) }}</div>
            <div class="doc-number">{{ invoice.number }}</div>
        </div>
        <div class="header-right">
            <div class="doc-meta">
                <div>{{ pdf_label('date_label', labelOverrides|default(null), locale|default('ro')) }}: {{ invoice.issueDate|date('d.m.Y') }}</div>
                {% if invoice.fiscalNumber %}
                    <div>{{ 'fiscal_number'|trans({}, 'pdf', locale|default('ro')) }}: {{ invoice.fiscalNumber }}</div>
                {% endif %}
                {% if invoice.cashRegisterName %}
                    <div>{{ 'cash_register'|trans({}, 'pdf', locale|default('ro')) }}: {{ invoice.cashRegisterName }}</div>
                {% endif %}
            </div>
        </div>
    </div>

    {% include 'documents/pdf/_parties.html.twig' with {supplierLabel: 'supplier_sender'|trans({}, 'pdf', locale|default('ro')), clientLabel: 'client_receiver'|trans({}, 'pdf', locale|default('ro'))} %}

    {% include 'documents/pdf/_lines_table.html.twig' with {showPrices: true, hideVat: false, hasProductCodes: hasProductCodes|default(false), hasLineDiscounts: hasLineDiscounts|default(false)} %}

    {% include 'documents/pdf/_totals.html.twig' %}

    {% if invoice.paymentMethod is defined and invoice.paymentMethod %}
    <div class="payment-info">
        <div class="payment-info-title">{{ 'payment_method'|trans({}, 'pdf', locale|default('ro')) }}:</div>
        {% if invoice.paymentMethod == 'cash' %}Numerar
        {% elseif invoice.paymentMethod == 'card' %}Card
        {% elseif invoice.paymentMethod == 'meal_ticket' %}Tichete de masa
        {% elseif invoice.paymentMethod == 'mixed' %}Mixt
        {% else %}{{ invoice.paymentMethod }}
        {% endif %}
        {% if invoice.paymentMethod == 'mixed' %}
            <br>
            {% if invoice.cashPayment %}Numerar: {{ invoice.cashPayment }} {{ invoice.currency }}{% endif %}
            {% if invoice.cardPayment %} | Card: {{ invoice.cardPayment }} {{ invoice.currency }}{% endif %}
            {% if invoice.otherPayment %} | Altele: {{ invoice.otherPayment }} {{ invoice.currency }}{% endif %}
        {% endif %}
    </div>
    {% endif %}

    {% include 'documents/pdf/_notes.html.twig' %}

    {% include 'documents/pdf/_footer.html.twig' %}
{% endblock %}
