apiVersion: v1
kind: Service
metadata:
  name: {{ include "storno.frontendServiceName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "storno.componentLabels" (dict "component" "frontend" "context" .) | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "storno.componentSelectorLabels" (dict "component" "frontend" "context" .) | nindent 4 }}
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
