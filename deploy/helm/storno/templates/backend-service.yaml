apiVersion: v1
kind: Service
metadata:
  name: {{ include "storno.backendServiceName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "storno.componentLabels" (dict "component" "backend" "context" .) | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "storno.componentSelectorLabels" (dict "component" "backend" "context" .) | nindent 4 }}
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
